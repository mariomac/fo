#ifndef _CALCULO_H_
#define _CALCULO_H_

// También podemos hacer #include, si se necesita

#define N 10

// También podemos definir typedef struct, si se necesita

double potencia(double a, int b);
long factorial(int n);
double exponencial(int x);


#endif
